{
  "name": "ui",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "e2e": "cargo build -p orchestrator -p factory && playwright test"
  },
  "dependencies": {
    "@icp-sdk/auth": "^9.0.0",
    "@ldclabs/ic-auth": "^0.5.1",
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@preact/preset-vite": "^2.10.0",
    "@testing-library/preact": "^3.2.4",
    "jsdom": "^27.0.0",
    "vite": "^8.3.0",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^5.0.0"
  }
}
