// Close icon for a dialog: a direct child of the dialog, pinned to its top-right while the content scrolls.
export const Close = ({ onClick }) => <div class="close"><button aria-label="Close" title="Close" onClick={onClick}>×</button></div>;
