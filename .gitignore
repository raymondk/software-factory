# Claude Code local/personal settings
.claude/

# Synced skill content (regenerated from skills-lock.json)
.agents/skills/

# OS
.DS_Store

# Rust
/target

# Binaries staged for the worker image
/dist

# A Worker's Workspace, when one is run from the repository root
/factory-workspace

# Secrets merged over factory.toml / provider.toml
*.secrets.toml

# SQLite
*.db
*.db-*

# UI
crates/orchestrator/ui/node_modules
crates/orchestrator/ui/dist
crates/orchestrator/ui/test-results
